spring:
  profiles:
    active: test

  jpa:
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 10
    open-in-view: false

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: info